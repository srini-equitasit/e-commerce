apiVersion: v1
kind: Service
metadata:
  name: EcommerceGateway
#   namespace: eks-example
  labels:
    app: EcommerceGateway
spec:
  type: NodePort
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 9080
      targetPort: 9080
  selector:
    app: EcommerceGateway