apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.product_seller.name }}
  namespace: {{ .Values.namespace }}
#   namespace: eks-example
  labels:
    app: {{ .Values.product_seller.name }}
spec:
#   type: LoadBalancer
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 9083
      targetPort: {{ .Values.product_seller.port }}
  selector:
    app: {{ .Values.product_seller.name }}