apiVersion: v1
kind: Service
metadata:
  name: e_commerce_ang
#   namespace: eks-example
  labels:
    app: e_commerce_ang
spec:
   type: NodePort
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 80
      targetPort: 80
  selector:
    app: e_commerce_ang