apiVersion: v1
kind: Service
metadata:
  name: EcommerceAng
#   namespace: eks-example
  labels:
    app: EcommerceAng
spec:
   type: NodePort
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 80
      targetPort: 80
  selector:
    app: EcommerceAng