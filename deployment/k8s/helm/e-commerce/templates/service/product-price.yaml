apiVersion: v1
kind: Service
metadata:
  name: product_price
#   namespace: eks-example
  labels:
    app: product_price
spec:
#   type: LoadBalancer
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 9082
      targetPort: 9082
  selector:
    app: product_price