apiVersion: v1
kind: Service
metadata:
  name: ProductPrice
#   namespace: eks-example
  labels:
    app: ProductPrice
spec:
#   type: LoadBalancer
  ports:
    - protocol: TCP
      # nodePort: 30101
      port: 9082
      targetPort: 9082
  selector:
    app: ProductPrice