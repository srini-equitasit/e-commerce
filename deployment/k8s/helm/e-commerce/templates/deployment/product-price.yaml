apiVersion: apps/v1
kind: Deployment
metadata:
  name: ProductPrice
  # namespace: eks-example
  labels:
    app: ProductPrice
  annotations:
    # Container Image Automated Updates
    flux.weave.works/automated: "true"
    # do not apply this manifest on the cluster
    #flux.weave.works/ignore: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ProductPrice
  template:
    metadata:
      labels:
        app: ProductPrice
    spec:
      containers:
      - name: ProductPrice
        image: {{ .Values.ProductPrice.image }}:{{ .Values.version }}
        # imagePullPolicy: IfNotPresent
        imagePullPolicy: Always
        ports:
        - containerPort: 9082
          name: http
          protocol: TCP
        livenessProbe:
          httpGet:
            path: actuator/health
            port: http
        readinessProbe:
          httpGet:
            path: actuator/health
            port: http