apiVersion: apps/v1
kind: Deployment
metadata:
  name: product_seller
  # namespace: eks-example
  labels:
    app: product_seller
  annotations:
    # Container Image Automated Updates
    flux.weave.works/automated: "true"
    # do not apply this manifest on the cluster
    #flux.weave.works/ignore: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product_seller
  template:
    metadata:
      labels:
        app: product_seller
    spec:
      containers:
      - name: product_seller
        image: {{ .Values.product_seller.image }}:{{ .Values.version }}
        # imagePullPolicy: IfNotPresent
        imagePullPolicy: Always
        ports:
        - containerPort: 9083
          name: http
          protocol: TCP
        livenessProbe:
          httpGet:
            path: actuator/health
            port: http
        readinessProbe:
          httpGet:
            path: actuator/health
            port: http