spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ${db_pwd}
    url: ${db_url}
    username: ${db_user}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true